import { FormInputProps } from 'semantic-ui-react';

const phoneProps: FormInputProps = {
  placeholder: '電話番号',
  type: 'tel',
  lable: '電話番号',
};
const phoneIconProps: FormInputProps = {
  icon: 'tel',
  iconPosition: 'left',
};

export { phoneProps, phoneIconProps };
