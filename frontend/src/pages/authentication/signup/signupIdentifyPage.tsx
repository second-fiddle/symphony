import { VFC } from 'react';
import { SignupIdentify } from 'biz/authentication/signup/signupIentify';

/**
 * 本人確認約ページ
 */
const SignupIdentifyPage: VFC = () => <SignupIdentify />;

export default SignupIdentifyPage;
